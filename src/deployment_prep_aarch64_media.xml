<?xml version="1.0" encoding="UTF-8"?>
<sect1 xmlns="http://docbook.org/ns/docbook"
       xmlns:its="http://www.w3.org/2005/11/its"
       xmlns:xi="http://www.w3.org/2001/XInclude"
       xmlns:xlink="http://www.w3.org/1999/xlink"
       version="5.2"
       xml:id="sec-aarch64-media">
   <title>Booting the system</title>
   <info/>
   <!--
  taroth 2019-07-31: CAVE - same section content is used in
  deployment_prep_x86_media.xml
  -->
   <para>This section gives an overview of the steps required for the complete installation of <phrase role="productname">SUSE® Linux Enterprise Server</phrase>.</para>
   <para>Unlike previous SLE products, the entire SLE <phrase role="productnumber">15 SP7</phrase> product line can be installed using the Unified Installer. For details about the changes since SUSE Linux Enterprise 15 and which media to download for installation, see <xref linkend="sec-planning-leanos"/>.</para>
   <para>For a full description of how to install and configure the system with YaST, refer to <xref linkend="part-deployment"/>.</para>
   <important>
      <title>Hardware support updates</title>
      <para>When using recent hardware, it can be necessary to boot the system with a newer kernel from a <literal>Kernel Update ISO</literal> image. For details, refer to <xref linkend="cha-kiso"/>.</para>
   </important>
   <procedure>
      <step>
         <para>Prepare the installation media.</para>
         <variablelist>
            <varlistentry>
               <term>USB Flash Drive</term>
               <listitem>
                  <para>This is the simplest way to start the installation. To create a bootable flash disk, you need to copy a DVD image to the device using the <command>dd</command> command. The flash disk must not be mounted, and all data on the device will be erased.</para>
                  <screen><prompt role="root">#</prompt><command>dd</command> if=<replaceable>PATH_TO_ISO_IMAGE</replaceable> of=<replaceable>USB_STORAGE_DEVICE</replaceable> bs=4M</screen>
               </listitem>
            </varlistentry>
            <varlistentry>
               <term>Network booting</term>
               <listitem>
                  <para>If the target computer's firmware supports it, you can boot the computer from the network and install from a server. This booting method requires a boot server that provides the needed boot images over the network. The exact protocol depends on your hardware. Commonly you need several services, such as TFTP and DHCP or PXE boot. For details, read <xref linkend="cha-deployment-prep-pxe"/>.</para>
                  <para>It is possible to install from many common network protocols, such as NFS, HTTP, FTP, or SMB. For more information on how to perform such an installation, refer to <xref linkend="cha-remote-installation"/>.</para>
               </listitem>
            </varlistentry>
         </variablelist>
      </step>
      <step>
         <para>Configure the target system firmware to boot the medium you chose. Refer to the documentation of your hardware vendor about how to configure the correct boot order.</para>
      </step>
      <step>
         <para>Set the boot parameters required for your installation control method. An overview of the different methods is provided in <xref linkend="sec-aarch64-prep-installation-methods"/>. A list of boot parameters is available in <xref linkend="cha-boot-parameters"/>.</para>
      </step>
      <step>
         <para>Perform the installation as described in <xref linkend="cha-install"/>. The system needs to restart after the installation is finished.</para>
      </step>
      <step>
         <para>Optional: Change the boot order of the system to directly boot from the medium to which <phrase role="productname">SUSE Linux Enterprise Server</phrase> has been installed. If the system boots from the installation medium, the first boot parameter will be to boot the installed system.</para>
      </step>
   </procedure>
</sect1>
